" Enable line numbers
set number

" Enable relative line numbers
set relativenumber

" Set tab width to 4 spaces
set tabstop=4
set shiftwidth=4
set expandtab

" Enable syntax highlighting
syntax enable

" Show line and column number in the status line
set ruler

" Enable line wrapping
set wrap

" Enable line wrapping at word boundaries
set linebreak

" Enable mouse support (optional)
set mouse=a

" Highlight search results as you type
set incsearch

" Ignore case when searching
set ignorecase

" Highlight search results case-sensitive if you use uppercase letters
set smartcase

" Enable line numbers in the gutter
set number

" Enable line number relative to the cursor line
set relativenumber

" Enable line number in the status line
set ruler

" Enable syntax highlighting
syntax enable

" Enable mouse support (optional)
set mouse=a

" Highlight search results as you type
set incsearch

" Ignore case when searching
set ignorecase

" Highlight search results case-sensitive if you use uppercase letters
set smartcase

" Set colorscheme (change 'desert' to your preferred colorscheme)
colorscheme desert

" mkdir -p ~/.vim/pack/vendor/start
" git clone https://github.com/morhetz/gruvbox.git ~/.vim/pack/vendor/start/gruvbox
"colorscheme gruvbox

